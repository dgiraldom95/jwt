{
  "name": "viaje_be",
  "license": "UNLICENSED",
  "version": "0.0.0",
  "scripts": {
    "start": "npm run build && cross-env NODE_PATH=./dist node dist/server.js",
    "start:dev": "NODE_PATH=./src ts-node src/server.ts",
    "debug": "node --inspect -r ts-node src/server.ts",
    "build": "tsc",
    "test": "jest --forceExit --detectOpenHandles",
    "lint": "tslint -p tsconfig.json -c tslint.json"
  },
  "dependencies": {
    "@types/bcrypt": "^3.0.0",
    "@types/express": "^4.17.2",
    "@types/jsonwebtoken": "^8.3.7",
    "@types/mongodb": "^3.3.16",
    "@types/node": "^13.7.6",
    "bcrypt": "^5.0.0",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "mongodb": "^3.5.3",
    "ts-node": "^8.6.2",
    "typescript": "^3.8.2"
  },
  "devDependencies": {
    "cross-env": "^7.0.0"
  }
}
